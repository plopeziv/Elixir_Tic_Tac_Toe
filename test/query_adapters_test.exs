defmodule QueryAdaptersTest do
  # use ExUnit.Case, async: false
  # import ExUnit.CaptureIO
  # import Mock
  # doctest QueryAdapters

  # setup do
  #   :ok = Ecto.Adapters.SQL.Sandbox.checkout(TTT.Repo)
  # end

  # test "Query for Player" do
  #   assert QueryAdapters.query_player_token("Matt") == "M"
  # end

  # test "Is player in database returns True" do
  #   assert QueryAdapters.is_player_in_db("Pedro") == true
  # end

  # test "Is player in database returns False" do
  #   assert QueryAdapters.is_player_in_db("Sunny") == false
  # end

  # test "Ability to remove players" do
  #   QueryAdapters.remove_player("Pedro")

  #   assert QueryAdapters.is_player_in_db("Pedro") == false

  #   QueryAdapters.create_player("Pedro", "P")
  # end

  # test "Remove player  does nothing if player is not in database" do
  #   assert QueryAdapters.remove_player("Chuck") == nil
  # end

  # test "New player is inserted to the database" do
  #   assert QueryAdapters.create_player("Brandon", "B")

  #   assert QueryAdapters.is_player_in_db("Brandon") == true

  #   QueryAdapters.remove_player("Brandon")
  # end

  # test "New player  does nothing if player is in database" do
  #   assert QueryAdapters.create_player("Pedro", "G") == nil
  # end
end
